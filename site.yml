---
- name: Playbook to install and configure Satellite Server
  # hosts: "{{ host | default('localhost') }}"
  hosts: localhost
  become: false
  vars:

    val1: "{{ lookup('file', uc_content_1 ) }}"
    val2: "{{ lookup('file', uc_content_2 ) }}"


  tasks:

    - name: Some debug
      ansible.builtin.debug:
        msg: content 1 is {{ val1 }}  content 2 is {{ val2 }}  

