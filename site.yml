---
- name: Playbook to install and configure Satellite Server
  # hosts: "{{ host | default('localhost') }}"
  hosts: localhost
  become: false

  tasks:

#    - name: Some debug
#      ansible.builtin.debug:
#        msg: env is {{ lookup('env', 'passwd' ) }} via extra-var {{ passwd }} of via extra_passwd {{ extra_passwd  }}

    - name: Some debug file
      ansible.builtin.debug:
        msg: file_content is {{ lookup('file', secret_1 ) }} 

    - name: Some debug extra_vars
      ansible.builtin.debug:
        msg: file_name on master is {{ secret_1  }} 

    - name: Some debug env 
      ansible.builtin.debug:
        msg: env var uc_content is {{ lookup('env', 'secret_1' ) }} 
